RFC SLOT GAME - ASSET HYGIENE AUDIT & CLEANUP REPORT
=======================================================
Date: 2026-05-20
Scope: /home/javier/rfc/UPLOAD_THIS_RFC_FRONT_END/
Performed by: Grok Build (asset hygiene subagent)

SUMMARY
-------
- Scanned entire tree (excluding .git during searches) for junk per spec: *.bak, *.kra, *~, "New Folder", design HTML fragments, empty dirs (fighter-max-idle), duplicate fighter art / sprite variants.
- Identified 40+ non-shipping / junk / design / build / dev artifacts.
- Created design/ archive dir.
- Moved all identified items (no deletions of critical runtime assets).
- Ensured symbols/moves/ exists (per submission-manifest.json) with placeholder NOTE.txt.
- Verified: no remaining bak/kra/~/(1)/(2) patterns or empty dirs in active tree.
- No runtime references broken (grepped post-move; only dead references were in archived .bak and design files).
- Conservative approach: left documented items (main animation-pack/ host sprites + foot-offsets, core build configs, README) in place even if currently unreferenced. Large obvious non-shipping (node_modules, dist, .claude) archived.

PROPOSED (AND EXECUTED) MOVES TO design/
----------------------------------------
Root-level junk:
  - index.html.bak
  - premium-spin-button.html
  - vs-now.png

assets/animation-pack/ junk & duplicates:
  - host-bigwin.png.bak
  - host-idle.png.bak
  - host-win.png.bak
  - sprite-max-px-frames-36-rows-6-cols-6 (1)/  [entire duplicate variant + json/png]
  - sprite-max-px-frames-36-rows-6-cols-6 (2)/  [entire duplicate variant + json/png]

assets/ui/ design fragments, temp, empty:
  - buybonus.html
  - CHAMPIONSHIP MULTIPLIER.html
  - controls.html
  - pick your fighter.html
  - pickfighter.html
  - RIVAL CALLOUT.html
  - spin-button-redesign.html
  - New Folder/ (empty)
  - reel-frame.webp-autosave.kra
  - reel-frame.webp~

art/ source junk:
  - image (1).jpg
  - image (2).jpg
  - image.jpg
  (rfc_bg.png kept - referenced)

character/ (unused design scraps with truncated filenames):
  - entire directory

assets/fighter-animations/ duplicate/unused fighter art:
  - grok.gif

assets/fighter-select/ duplicate/unused fighter art variants (kept only referenced webp + *_pick.gif):
  - champ.png, champ_opt.jpg, champ_selected.gif
  - chief.png, chief_opt.jpg, chief_selected.gif
  - mcwhiskey.png, mcwhiskey_opt.jpg, mcwhiskey_selected.gif
  - rage.png, rage_opt.jpg, rage_selected.gif

symbols/ duplicate/unused fighter art (kept only sym_* used by reels):
  - champ.png, chief.png, mcwhiskey.png, rage.png

Empty dirs (removed):
  - assets/fighter-max-idle/  (was empty; fighter-max/ + win-* dirs kept - critical animation frames)
  - assets/ui/New Folder/ (moved above)

Large non-shipping / dev / build artifacts (archived for clean upload package):
  - dist/ (entire; build output with hashed duplicates)
  - node_modules/ (entire; dev deps)
  - .claude/ (AI tooling)
  - .playwright-mcp (test tooling)
  - scripts/ (dev/optimize scripts)

POST-CLEAN ACTIVE STRUCTURE (key paths kept for runtime)
--------------------------------------------------------
- index.html (entry)
- stake-engine-bridge.js (RGS)
- submission-manifest.json
- README.md
- package.json / package-lock.json / vite.config.js / tsconfig.json (build)
- thumbnail.jpg
- art/rfc_bg.png
- assets/
    animation-pack/ (core files only: hosts + main sprite + foot-offsets)
    fighter-animations/ (7 GIFs, grok removed)
    fighter-max*/ (all frame sequences - critical)
    fighter-select/ (only 8 files: 4 webp + 4 pick gifs)
    ui/ (only 4 core: bottom-hud-shell.webp, fighter-picker-exact.png, reel-frame.webp, spin-medallion.webp)
    intro_*.mp4, *_Crowned.mp3, background_loop.mp3
- symbols/ (only 16 sym_*.png + moves/)
- symbols/moves/ (with NOTE.txt placeholder)

symbols/moves/ ACTION
---------------------
- Created per manifest "moveSymbolsDirectory": "symbols/moves/"
- Added NOTE.txt documenting its purpose (premium Bonus 2 move symbols).
- Ready for future artwork drop. No code currently loads from it (manifest-only).

VERIFICATION STEPS PERFORMED
----------------------------
1. Pre-clean: multiple find + list_dir + grep across all files for patterns and references.
2. Post-move: re-ran junk pattern find (0 results in active tree).
3. Grep for moved filenames in non-design/ tree (0 hits).
4. Confirmed critical assets intact via list_dir (frames, webps, mp3s, videos, syms, fighter anims).
5. symbols/moves/ + NOTE.txt present.
6. No empty dirs left in active tree.

NOTES / RECOMMENDATIONS
-----------------------
- animation-pack/ host sprites + main sprite dir + foot-offsets left in place (per README docs) despite no active references in index.html (old host anims appear replaced by fighter GIFs + max sprite system). Consider removing in future polish if confirmed dead.
- dist/ and node_modules archived (standard for submissions; .gitignore already excluded them).
- If a production build is required for submission, it can be re-run after npm install (node_modules can be restored outside design/).
- fighter-max/ uses frames 021-056 for idle (36 frames @ start 21); win anims in sibling folders - all preserved.
- All moves used `mv` (no data loss); originals now safely in design/.

CLEANUP COMPLETE. Runtime-critical assets untouched. Package ready for leaner upload/submission.

-- End of Report --